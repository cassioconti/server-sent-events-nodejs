{
  "name": "server-sent-events-nodejs",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc && cp client/index.html dist/client && cp client/main.css dist/client",
    "lint": "tslint --project ./",
    "start": "node dist/server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4"
  },
  "devDependencies": {
    "@types/express": "^4.16.0",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  }
}